# sf-metadata-kg-builder
An application that parses Salesforce metadata bundles, converts raw XML and Apex artifacts into canonical metadata JSON, and generates a Metafore-ready knowledge graph with nodes, edges, relationships, classifications, provenance, and validation reports.
